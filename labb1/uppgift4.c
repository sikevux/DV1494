#include <stdio.h>

int main() {
	int side1 = 10;
	int side2 = 30;
	int area = side1 * side2;
	int circumference = (side1 * 2) + (side2 * 2);

	printf("Rektanglen har sidorna %d och %d, vilket gör att arean blir %d och omkretsen %d\n", side1, side2, area, circumference);





	return 0;
}
